# Prisma-only env file (used by prisma.config.ts)
#
# Copy to .env.prisma and point at a LOCAL or DEV database.
# Do NOT put production credentials here.

# Local Postgres example (docker-compose.dev.yml)
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/homehero?schema=public
DATABASE_PRIVATE_URL=postgresql://postgres:postgres@localhost:5432/homehero?schema=public
